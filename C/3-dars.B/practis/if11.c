#include <stdio.h>
#include <stdlib.h>

int main(){
    system("cls");
    int a,b,c;
    printf("A kiriting >> ");
    scanf("%d", &a);
    printf("B kiriting >> ");
    scanf("%d", &b);

    if (a == b){
    }else{
        if (a>b){
            
        }
    }
    printf("A=%d B=%d", a, b);

}